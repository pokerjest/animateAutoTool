<script setup lang="ts">import { AlertTriangle } from '@lucide/vue'; import { DialogClose, DialogContent, DialogDescription, DialogOverlay, DialogPortal, DialogRoot, DialogTitle } from 'reka-ui'; import AsyncButton from './AsyncButton.vue'; defineProps<{ open:boolean; title:string; description:string; confirmLabel?:string; danger?:boolean; loading?:boolean; loadingLabel?:string }>(); defineEmits<{ 'update:open':[value:boolean]; confirm:[] }>();</script>
<template><DialogRoot :open="open" @update:open="$emit('update:open',$event)"><DialogPortal><DialogOverlay class="fixed inset-0 z-[80] bg-black/45 backdrop-blur-sm"/><DialogContent class="panel fixed left-1/2 top-1/2 z-[81] w-[min(92vw,480px)] -translate-x-1/2 -translate-y-1/2 p-6"><AlertTriangle class="mb-4" :class="danger?'text-[var(--danger)]':'text-[var(--warning)]'"/><DialogTitle class="text-xl font-black">{{ title }}</DialogTitle><DialogDescription class="muted mt-2 text-sm leading-6">{{ description }}</DialogDescription><div class="mt-6 flex justify-end gap-2"><DialogClose class="btn btn-secondary" :disabled="loading">取消</DialogClose><AsyncButton class="btn" :class="danger?'btn-danger':'btn-primary'" :loading="loading" :loading-label="loadingLabel||'处理中…'" @click="$emit('confirm')">{{ confirmLabel||'确认' }}</AsyncButton></div></DialogContent></DialogPortal></DialogRoot></template>
